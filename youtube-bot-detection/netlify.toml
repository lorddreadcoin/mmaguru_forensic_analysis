[build]
  # No build command needed for static HTML
  command = ""
  # Serve files from streamlit_app directory where our dashboard is
  publish = "streamlit_app"

[build.environment]
  # Override Next.js detection
  NETLIFY_NEXT_PLUGIN_SKIP = "true"
