[build]
  # No build command - this is static HTML
  command = "echo 'No build needed'"
  # Serve files from streamlit_app directory
  publish = "streamlit_app"

[[headers]]
  for = "/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"
